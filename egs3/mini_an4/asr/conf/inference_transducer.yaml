##########################################################
#                   Inference SETTING                    #
##########################################################
# Common usage:
#   python run.py --stages infer \
#       --training_config conf/training_asr_transducer.yaml \
#       --inference_config conf/inference_transducer.yaml
#
##########################################################
#                    PATHS AND INPUTS                    #
##########################################################
# This recipe omits `exp_tag`, `exp_dir`, and `inference_dir` on purpose.
# `run.py` copies `training_config.exp_tag` and `training_config.exp_dir` here
# before resolving `${exp_dir}`.
#
# The default output directory is `${exp_dir}/inference_transducer`.
# For standalone `infer`, add either `exp_tag` or a concrete `exp_dir` here.
# Add `inference_dir` only when you want a different output directory.

##########################################################
#                    DATASET DEFINITION                  #
##########################################################
# Each test entry is a dataset reference consumed by `DataOrganizer`.
# - `name` becomes `${inference_dir}/<name>/`
# - `data_src` is omitted, so ESPnet3 loads
#   `${recipe_dir}/dataset/__init__.py`
# - `data_src_args` is passed directly to `MiniAn4Dataset(...)`
#
# Dataset splits:
#   test:
#     - split="valid" from `MiniAn4Dataset`
#     - split="test" from `MiniAn4Dataset`
# Notes:
#   - actual data sources are defined via `${recipe_dir}`
dataset:
  test:
    - name: valid
      data_src_args:
        split: valid
    - name: test
      data_src_args:
        split: test

##########################################################
#                    MODEL AND OUTPUT                    #
##########################################################
model:
  _target_: espnet2.bin.asr_transducer_inference.Speech2Text
  asr_train_config: ${exp_dir}/config.yaml
  asr_model_file: ${exp_dir}/last.ckpt
  beam_size: 1

input_key: speech
output_fn: src.inference.build_output_transducer
